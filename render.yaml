services:
  - type: web
    name: pandoc-export-server
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    initBuildScript: |
      apt-get update && apt-get install -y pandoc
